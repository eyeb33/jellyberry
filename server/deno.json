{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-ffi --watch main.ts",
    "start": "deno run --allow-net --allow-env --allow-ffi main.ts"
  },
  "compilerOptions": {
    "lib": ["deno.window"]
  }
}
